function petShop(dogsCount, animalCount) {

    let dogsSum = dogsCount * 2.5;
    let animalsSum = animalCount * 4;
    let totalSum = dogsSum + animalsSum;

    console.log(`${totalSum} lv.`);
}
petShop("13", "9")
